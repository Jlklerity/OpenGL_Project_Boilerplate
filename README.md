# OpenGL_Project_Boilerplate
Boilerplate for making OpenGL project


how to build in the project folder(for mac):
rm -rf build
cmake -S . -B build
cmake --build build -j
./build/bin/GameApp

for windows cmake in vs 2022:
ctrl+shift+b in cmakelists.txt file